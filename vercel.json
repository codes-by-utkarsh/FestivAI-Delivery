{
  "cleanUrls": true,
  "buildCommand": "cd frontend_app && npm install && npm run build",
  "outputDirectory": "frontend_app/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
